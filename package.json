{
  "name": "pirateborg",
  "version": "1.0.0",
  "description": "Official system for PIRATE BORG on Foundry VTT.",
  "main": "./module/pirateborg.js",
  "scripts": {
    "build:clean": "node ./utils/packs.mjs package clean",
    "build:db": "node ./utils/packs.mjs package pack",
    "build:json": "node ./utils/packs.mjs package unpack",
    "lint": "eslint --fix --ext .js ./module --format node_modules/eslint-plugin-i18n-json/formatter.js lang/*",
    "precommit": "npm run validate",
    "prepare": "husky install",
    "pretty": "prettier --write .",
    "pretty-check": "prettier --check .",
    "sass": "sass ./scss:./css",
    "validate": "npm-run-all pretty-check lint",
    "watch": "sass --watch ./scss:./css",
    "pack": "npm run build:db && node -e \"const fs=require('fs'); const s=JSON.parse(fs.readFileSync('system.json')); s.version=s.version.split('-')[0]+'-'+require('child_process').execSync('git branch --show-current',{encoding:'utf8'}).trim()+'-'+require('child_process').execSync('git rev-parse --short HEAD',{encoding:'utf8'}).trim(); fs.writeFileSync('system.json',JSON.stringify(s,null,2)+'\\n'); console.log('Updated version:',s.version)\" && zip -r ./system.zip CHANGELOG.md LICENSE.MB3PL LICENSE.MIT how-to-use-this-system.md README.md system.json template.json css/ fonts/ icons/ lang/ module/ packs/ templates/ tokens/ ui/"
  },
  "repository": "https://github.com/Limithron-Foundry-VTT/pirate-borg-system",
  "author": "maqloner",
  "license": "MIT",
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0.3",
    "@typhonjs-fvtt/eslint-config-foundry.js": "^0.8.0",
    "eslint": "^8.17.0",
    "eslint-plugin-i18n-json": "^3.1.0",
    "eslint-plugin-import": "^2.26.0",
    "fancy-log": "^2.0.0",
    "husky": "^7.0.4",
    "npm-run-all": "^4.1.5",
    "path": "^0.12.7",
    "prettier": "^2.7.1",
    "sass": "^1.49.0",
    "yargs": "^17.7.2"
  }
}
